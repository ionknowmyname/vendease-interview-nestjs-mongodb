export enum ENV {
  MONGO_URI = 'MONGO_URI',
  NODE_ENV = 'NODE_ENV',
}

export enum GENDER {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
}

export enum STATUS {
  ACTIVE = 'ACTIVE',
  DEAD = 'DEAD',
  UNKNOWN = 'UNKNOWN',
}
